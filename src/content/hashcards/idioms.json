{
  "cards": [
    {
      "type": "markdown",
      "question": "Running minimum\n\nScan a sequence once and track the smallest value seen so far.",
      "answer": "```c\nint min_so_far = arr[0];\n\nfor (int i = 1; i < n; i++) {\nif (arr[i] < min_so_far) {\nmin_so_far = arr[i];\n}\n}\n```"
    },
    {
      "type": "markdown",
      "question": "Running maximum\n\nScan a sequence once and track the largest value seen so far.",
      "answer": "```c\nint max_so_far = arr[0];\n\nfor (int i = 1; i < n; i++) {\nif (arr[i] > max_so_far) {\nmax_so_far = arr[i];\n}\n}\n```"
    }
  ]
}
