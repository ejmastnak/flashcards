{"cards": [{"type": "markdown", "question": "Binary search with open search interval\n\nFind the index of target in a sorted array, or return `-1` if absent.\nUse a half-open search interval.", "answer": "```c\nint l = 0, r = n;\n\nwhile (l < r) {\n    int m = l + (r - l) / 2;\n    if (arr[m] == target) return m;\n    else if (arr[m] < target) l = m + 1;\n    else r = m;\n}\n\nreturn -1;\n```"}, {"type": "markdown", "question": "Binary search with closed search interval\n\nFind the index of target in a sorted array, or return `-1` if absent.\nUse a closed search interval.", "answer": "```c\nint l = 0, r = n - 1;\n\nwhile (l <= r) {\n    int m = l + (r - l) / 2;\n\n    if (arr[m] == target) return m;\n    else if (arr[m] < target) l = m + 1;\n    else r = m - 1;\n   \n}\n\nreturn -1;\n```"}, {"type": "markdown", "question": "Lower bound\n\nIn a sorted array with duplicates, find the lower bound of a target.", "answer": "```c\nint l = 0, r = n;\n\nwhile (l < r) {\n    int m = l + (r - l) / 2;\n    if (arr[m] < target) l = m + 1;\n    else r = m;\n}\n\n// Loop ends with `l == lower_bound(target)`\nreturn l;\n```\n\nComment: loop ends with `l == n` if all elements are `< target`."}, {"type": "markdown", "question": "Upper bound\n\nIn a sorted array with duplicates, find the upper bound of a target.", "answer": "```c\nint l = 0, r = n;\n\nwhile (l < r) {\n    int m = l + (r - l) / 2;\n    if (arr[m] <= target) l = m + 1;\n    else r = m;\n}\n\n// Loop ends with `l == upper_bound(target)`\nreturn l;\n```\n\nComment: loop ends with `l == 0` if all elements are `> target`."}]}